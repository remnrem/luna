CXX_STD = CXX11

PKG_CPPFLAGS=-I./include/ -I$(FFTW)/include/ 

# default libs (no LGBM)
PKG_LIBS=include/libluna.a -L$(FFTW)/lib/ -lfftw3

# for LGBM support, assuming system-wide install of LGBM
#PKG_LIBS=include/libluna.a -L$(FFTW)/lib/ -lfftw3 -l_lightgbm

# redundant
#PKG_CPPFLAGS=-I./include/ `pkg-config --cflags fftw3`
#PKG_LIBS=include/libluna.a `pkg-config --libs fftw3`
